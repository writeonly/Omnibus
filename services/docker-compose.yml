services:

  config-server:
    build:
      context: .
      dockerfile: config-server/Dockerfile
    container_name: config-server
    ports:
      - "8888:8888"
    environment:
      CONFIG_ENV: local
    volumes:
      - ./config-server/config:/workspace/config

  api-gateway:
    build:
      context: .
      dockerfile: api-gateway/Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - config-server
      - bidding-engine
      - workflow-orchestrator
    networks: [omnibus]

  bidding-engine:
    build:
      context: .
      dockerfile: ./bidding-engine/Dockerfile
    ports:
      - "8081:8081"
    depends_on: 
      - config-server
    networks: [omnibus]

  workflow-orchestrator:
    build:
      context: .
      dockerfile: ./workflow-orchestrator/Dockerfile
    ports:
      - "8082:8082"
    depends_on:
      - config-server
      - bidding-engine
    networks: [omnibus]

  event-archive:
    profiles:
      - legacy
    build:
      context: .
      dockerfile: ./services/event-archive/Dockerfile
    networks: [omnibus]

  outbox-relay-service:
    profiles:
      - legacy
    build:
      context: .
      dockerfile: ./outbox-relay-service/Dockerfile
    networks: [omnibus]

  cassandra-projection-service:
    profiles:
      - legacy
    build:
      context: .
      dockerfile: ./cassandra-projection-service/Dockerfile
    networks: [omnibus]

networks:
  omnibus:
