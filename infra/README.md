# Infrastructure

This directory is reserved for shared deployment assets such as:

- Kubernetes manifests
- Helm charts
- CI/CD pipeline templates
- environment overlays

For now the platform is bootstrapped with `docker-compose.yml` at the repository root.
