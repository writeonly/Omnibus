services:

  config-server:
    build:
      context: .
      dockerfile: server/config-server/Dockerfile
    container_name: config-server
    mem_limit: 512m
    ports:
      - "8888:8888"
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_ENV: local
      CONFIG_REPO_LOCATION: file:/workspace/config
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    volumes:
      - ./server/config-server/config:/workspace/config
    networks:
      - omnibus

  eureka-server:
    build:
      context: .
      dockerfile: server/eureka-server/Dockerfile
    container_name: eureka-server
    mem_limit: 512m
    ports:
      - "8761:8761"
    depends_on:
      - config-server
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    networks:
      - omnibus

  auth-server:
    build:
      context: .
      dockerfile: server/auth-server/Dockerfile
    container_name: auth-server
    mem_limit: 512m
    ports:
      - "9000:9000"
    depends_on:
      - config-server
      - eureka-server
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
      DB_URL: jdbc:postgresql://postgres:5432/auth_db
      POSTGRES_DB: auth_db
      DB_USERNAME: omnibus
      DB_PASSWORD: omnibus
      AUTH_ISSUER: http://auth-server:9000
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    networks:
      - omnibus

  api-gateway:
    build:
      context: .
      dockerfile: server/api-gateway/Dockerfile
    mem_limit: 512m
    ports:
      - "8080:8080"
    depends_on:
      - config-server
      - auth-server
      - eureka-server
      - rule-engine
      - workflow-engine
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
      AUTH_SERVER_URL: http://auth-server:9000
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      RULE_ENGINE_GRPC_URI: lb:h2c://rule-engine
      WORKFLOW_ENGINE_GRPC_URI: lb:h2c://workflow-engine
    networks:
      - omnibus

  audit-service:
    build:
      context: .
      dockerfile: service/audit-service/Dockerfile
    container_name: audit-service
    mem_limit: 512m
    ports:
      - "8081:8081"
    depends_on:
      - config-server
      - eureka-server
      - auth-server
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
      AUTH_SERVER_URL: http://auth-server:9000

      KAFKA_BOOTSTRAP_SERVERS: kafka:9092

      DB_URL: jdbc:postgresql://postgres:5432/audit_db
      DB_USERNAME: omnibus
      DB_PASSWORD: omnibus
      POSTGRES_DB: audit_db

      CASSANDRA_CONTACT_POINTS: cassandra:9042
      CASSANDRA_HOST: cassandra
      CASSANDRA_PORT: 9042
      CASSANDRA_KEYSPACE: audit

    networks:
      - omnibus

  user-service:
    build:
      context: .
      dockerfile: service/user-service/Dockerfile
    container_name: user-service
    mem_limit: 512m
    ports:
      - "8082:8082"
    depends_on:
      - config-server
      - eureka-server
      - auth-server
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
      AUTH_SERVER_URL: http://auth-server:9000
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092

      # jeśli używasz DB w user-service:
      DB_URL: jdbc:postgresql://postgres:5432/user_db
      DB_USERNAME: omnibus
      DB_PASSWORD: omnibus
      POSTGRES_DB: user_db
    networks:
      - omnibus

  rule-engine:
    build:
      context: .
      dockerfile: service/rule-engine/Dockerfile
    mem_limit: 512m
    ports:
      - "8083:8083"
    depends_on:
      - config-server
      - auth-server
      - eureka-server
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
      AUTH_SERVER_URL: http://auth-server:9000
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    networks:
      - omnibus

  workflow-engine:
    build:
      context: .
      dockerfile: service/workflow-engine/Dockerfile
    mem_limit: 512m
    ports:
      - "8084:8084"
    depends_on:
      - config-server
      - auth-server
      - eureka-server
      - rule-engine
    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx256m -XX:+UseSerialGC"
      CONFIG_SERVER_URL: http://name:password@config-server:8888
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka
      AUTH_SERVER_URL: http://auth-server:9000
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      BIDDING_ENGINE_GRPC_ADDRESS: discovery:///rule-engine
    networks:
      - omnibus

networks:
  omnibus:
    name: omnibus
